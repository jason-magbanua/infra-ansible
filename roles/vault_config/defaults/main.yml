---
vault_addr: "http://10.10.200.40:8200"
vault_db_host: "10.10.200.30"
vault_db_port: 5432
vault_db_name: postgres

# The following must be set via Ansible Vault (secrets/vault-vault.yml):
# vault_root_token: ""       # Vault root token — rotate to AppRole ASAP
# vault_db_user: ""          # PostgreSQL superuser for Vault DB plugin
# vault_db_pass: ""          # PostgreSQL password for vault_db_user
# vault_authentik_secret_key: ""   # Authentik SECRET_KEY
