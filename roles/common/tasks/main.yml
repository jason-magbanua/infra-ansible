---
- name: Install common packages
  ansible.builtin.apt:
    name:
      # baseline
      - net-tools
      - curl
      - wget
      - htop
      - strace
      # editors / pagers
      - vim
      - less
      # file / archive
      - rsync
      - unzip
      - tree
      - ncdu
      # process inspection
      - lsof
      - iotop
      - sysstat
      - psmisc
      # network
      - dnsutils
      - traceroute
      - mtr
      - iftop
      # data
      - jq
      - git
      # apt / repo
      - ca-certificates
      - apt-transport-https
      - gnupg
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Set bash history timestamp format
  ansible.builtin.copy:
    dest: /etc/profile.d/bash_history_timestamp.sh
    content: "export HISTTIMEFORMAT=\"%F %T \"\n"
    owner: root
    group: root
    mode: "0644"
