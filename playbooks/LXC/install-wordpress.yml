---
- name: Deploy WordPress on an LXD container
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    lxd_remote: wp-host1
    php_version: "8.3"
    wp_root: /var/www/html

  pre_tasks:

    - name: Ensure required variables are provided
      assert:
        that:
          - container_name | default("") != ""
          - domain | default("") != ""
        fail_msg: "container_name and domain must be provided via -e"

  tasks:

############################################################
# Detect WordPress
############################################################

    - name: Detect existing WordPress installation
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        test -f {{ wp_root }}/wp-config.php
      register: wp_installed
      failed_when: false
      changed_when: false

############################################################
# WordPress install
############################################################

    - name: Download WordPress
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        bash -c "cd /tmp && curl -L -O https://wordpress.org/latest.tar.gz && tar -xzf latest.tar.gz"
      when: wp_installed.rc != 0

    - name: Install WordPress files
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        bash -c "mkdir -p {{ wp_root }} && cp -r /tmp/wordpress/* {{ wp_root }}"
      when: wp_installed.rc != 0

    - name: Ensure WordPress ownership
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        chown -R www-data:www-data {{ wp_root }}

############################################################
# Database
############################################################

    - name: Generate DB credentials
      set_fact:
        wp_db_name: "wp_{{ lookup('password', '/dev/null length=12 chars=ascii_lowercase') }}"
        wp_db_user: "wp_{{ lookup('password', '/dev/null length=12 chars=ascii_lowercase') }}"
        wp_db_password: "{{ lookup('password', '/dev/null length=28 chars=ascii_letters,digits') }}"
      when: wp_installed.rc != 0

    - name: Load existing DB credentials from secrets file
      include_vars:
        file: "/opt/infra/secrets/{{ container_name }}-db.yml"
      when: wp_installed.rc == 0

    - name: Save DB credentials to secrets file
      copy:
        dest: "/opt/infra/secrets/{{ container_name }}-db.yml"
        mode: "0600"
        content: |
          # WordPress DB credentials for {{ container_name }} ({{ domain }})
          # Generated: {{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}
          wp_db_name: {{ wp_db_name }}
          wp_db_user: {{ wp_db_user }}
          wp_db_password: {{ wp_db_password }}
      when: wp_installed.rc != 0

    - name: Create database
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        mysql -e "CREATE DATABASE {{ wp_db_name }}"
      when: wp_installed.rc != 0

    - name: Create database user
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        mysql -e "
        CREATE USER '{{ wp_db_user }}'@'localhost'
        IDENTIFIED BY '{{ wp_db_password }}';
        GRANT ALL PRIVILEGES ON {{ wp_db_name }}.* TO
        '{{ wp_db_user }}'@'localhost';
        FLUSH PRIVILEGES;"
      when: wp_installed.rc != 0

############################################################
# WordPress configuration
############################################################

    - name: Render wp-config.php
      template:
        src: templates/wp-config.php.j2
        dest: /tmp/wp-config.php

    - name: Push wp-config.php to container
      command: >
        lxc file push --uid 33 --gid 33
        /tmp/wp-config.php
        {{ lxd_remote }}:{{ container_name }}{{ wp_root }}/wp-config.php

############################################################
# Nginx configuration
############################################################

    - name: Render nginx vhost config
      template:
        src: templates/nginx-vhost-basic.conf.j2
        dest: /tmp/nginx.conf

    - name: Push nginx vhost config to container
      command: >
        lxc file push --uid 0 --gid 0
        /tmp/nginx.conf
        {{ lxd_remote }}:{{ container_name }}/etc/nginx/sites-available/{{ domain }}

    - name: Enable nginx site
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        ln -sf /etc/nginx/sites-available/{{ domain }}
        /etc/nginx/sites-enabled/{{ domain }}

    - name: Remove default nginx site
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        rm -f /etc/nginx/sites-enabled/default

    - name: Test nginx config
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        nginx -t

    - name: Reload nginx
      command: >
        lxc exec {{ lxd_remote }}:{{ container_name }} --
        systemctl reload nginx

############################################################
# Summary
############################################################

    - name: Deployment summary
      debug:
        msg:
          - "Container : {{ container_name }}"
          - "Domain    : {{ domain }}"
          - "DB name   : {{ wp_db_name | default('(pre-existing)') }}"
          - "DB user   : {{ wp_db_user | default('(pre-existing)') }}"
          - "DB pass   : {{ wp_db_password | default('(pre-existing)') }}"
          - "Creds file: /opt/infra/secrets/{{ container_name }}-db.yml"
