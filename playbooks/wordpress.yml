---
# TODO: Deploy WordPress on VM hosts (after lemp.yml)
# This playbook is a stub — implement before running site.yml
- name: Install WordPress
  hosts: proxmox_vms
  become: true

  vars:
    wp_root: /var/www/html

  tasks:
    - name: Not yet implemented
      fail:
        msg: "wordpress.yml is not yet implemented"
